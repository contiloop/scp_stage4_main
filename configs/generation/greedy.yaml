# Greedy decoding for deterministic before/after comparison.
# Algorithm 1 line 12: (M_{k-1} + theta_probe)(s, greedy)
max_new_tokens: 512
do_sample: false
num_beams: 1
temperature: 1.0
top_p: 1.0
batch_size: 8
sort_by_input_length: true
